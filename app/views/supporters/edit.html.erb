<div class="max-w-3xl">
	<h1 class="text-2xl font-semibold text-slate-900 mb-6">Editar Apoiador</h1>
	<div class="rounded-xl bg-white border border-slate-200 p-6 shadow-sm mb-6">
		<%= render 'form', supporter: @supporter %>
	</div>
	<%= link_to 'Voltar', supporter_path(@supporter), class: 'text-sm font-medium text-slate-600 hover:text-slate-800' %>
</div>
