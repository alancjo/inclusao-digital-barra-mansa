<div class="mb-12 relative overflow-hidden rounded-2xl border border-slate-200 bg-gradient-to-br from-brand-50 via-white to-slate-50 p-8 shadow-sm">
  <div class="max-w-2xl">
    <h1 class="text-3xl font-bold tracking-tight text-slate-900 mb-4">Inclusão Digital Barra Mansa</h1>
    <p class="text-sm text-slate-600 leading-relaxed">Conectamos alunos de baixa renda a mentores voluntários de tecnologia, criando oportunidades reais de aprendizado, crescimento e inserção digital. Participe como mentor, aluno ou apoiador.</p>
    <div class="mt-6 flex flex-wrap gap-3">
      <%= link_to 'Quero Mentorar', new_mentor_path, class: 'inline-flex items-center rounded-md bg-brand-500 hover:bg-brand-600 text-white px-5 py-2 text-sm font-medium shadow' %>
      <%= link_to 'Sou Aluno', new_student_path, class: 'inline-flex items-center rounded-md bg-slate-800 hover:bg-slate-700 text-white px-5 py-2 text-sm font-medium shadow' %>
      <%= link_to 'Quero Apoiar', new_supporter_path, class: 'inline-flex items-center rounded-md border border-slate-300 hover:bg-white/70 bg-white text-slate-700 px-5 py-2 text-sm font-medium shadow-sm' %>
      <%= link_to 'Ver Conexões', match_path, class: 'inline-flex items-center rounded-md bg-slate-200 hover:bg-slate-300 text-slate-800 px-5 py-2 text-sm font-medium' %>
    </div>
  </div>
</div>

<%= render 'dashboard' %>

<div class="grid gap-12 lg:grid-cols-2">
  <div>
    <div class="flex items-center justify-between mb-6">
      <h2 class="text-xl font-semibold tracking-tight text-slate-900">Mentores</h2>
      <%= link_to 'Ver todos', mentors_path, class: 'inline-flex items-center rounded-md bg-brand-500 hover:bg-brand-600 text-white px-4 py-2 text-xs font-medium shadow' %>
    </div>
    <div class="grid gap-5 sm:grid-cols-2" data-animate-stagger>
      <% @mentors.first(6).each do |mentor| %>
        <div class="group rounded-xl bg-white border border-slate-200 shadow-sm hover:shadow-md transition p-5 flex flex-col">
          <div class="flex items-start justify-between mb-2">
            <h3 class="font-semibold text-slate-900 text-sm"><%= mentor.full_name %></h3>
            <% if mentor.availability.present? %>
              <span class="text-[9px] uppercase tracking-wide font-semibold bg-brand-100 text-brand-700 px-2 py-1 rounded"><%= mentor.availability %></span>
            <% end %>
          </div>
          <span class="inline-block text-[10px] font-bold tracking-wide bg-slate-900 text-white px-2 py-1 rounded mb-2"><%= mentor.expertise %></span>
          <p class="text-xs text-slate-600 flex-1"><%= mentor.bio.to_s.truncate(110) %></p>
          <div class="mt-3 text-[10px] text-slate-700 font-medium flex items-center gap-1">
            <span class="text-slate-400">📧</span> <%= mentor.contact_info %>
          </div>
          <div class="mt-3">
            <%= link_to 'Ver perfil completo →', mentor_path(mentor), class: 'text-[10px] font-semibold text-brand-600 hover:text-brand-700' %>
          </div>
        </div>
      <% end %>
      <% if @mentors.empty? %>
        <div class="col-span-full text-sm text-slate-500">Nenhum mentor cadastrado ainda.</div>
      <% end %>
    </div>
    <% if @mentors.size > 6 %>
      <div class="mt-4">
        <%= link_to 'Ver todos os mentores', mentors_path, class: 'text-xs font-medium text-brand-600 hover:text-brand-700' %>
      </div>
    <% end %>
  </div>
  <div>
    <div class="flex items-center justify-between mb-6">
      <h2 class="text-xl font-semibold tracking-tight text-slate-900">Alunos</h2>
      <%= link_to 'Ver todos', students_path, class: 'inline-flex items-center rounded-md bg-slate-800 hover:bg-slate-700 text-white px-4 py-2 text-xs font-medium shadow' %>
    </div>
    <div class="grid gap-5 sm:grid-cols-2" data-animate-stagger>
      <% @students.first(6).each do |student| %>
      <div class="rounded-xl bg-white border border-slate-200 p-5 shadow-sm hover:shadow-md transition flex flex-col">
        <div class="flex items-start justify-between mb-2">
          <h3 class="font-semibold text-slate-900 text-sm"><%= student.full_name %></h3>
          <% if student.proficiency_level.present? %>
            <span class="text-[9px] uppercase tracking-wide font-semibold bg-slate-100 text-slate-600 px-2 py-1 rounded"><%= student.proficiency_level %></span>
          <% end %>
        </div>
        <div class="mb-2 flex flex-wrap gap-1">
          <% student.interests_tokens.first(4).each do |i| %>
            <span class="text-[9px] font-medium bg-slate-900 text-white px-2 py-0.5 rounded"><%= i %></span>
          <% end %>
        </div>
        <p class="text-xs text-slate-600 flex-1"><%= student.learning_goal.to_s.truncate(90) %></p>
        <div class="mt-3 text-[10px] text-slate-700 font-medium flex items-center gap-1">
          <span class="text-slate-400">📧</span> <%= student.contact_info %>
        </div>
          <div class="mt-3">
            <%= link_to 'Ver perfil completo →', student_path(student), class: 'text-[10px] font-semibold text-brand-600 hover:text-brand-700' %>
          </div>
      </div>
    <% end %>
      <% if @students.empty? %>
        <div class="col-span-full text-sm text-slate-500">Nenhum aluno cadastrado ainda.</div>
      <% end %>
    </div>
    <% if @students.size > 6 %>
      <div class="mt-4">
        <%= link_to 'Ver todos os alunos', students_path, class: 'text-xs font-medium text-brand-600 hover:text-brand-700' %>
      </div>
    <% end %>
  </div>
</div>

<div class="mt-16 pt-12 border-t border-slate-200">
  <div class="text-center mb-8">
    <h2 class="text-2xl font-bold tracking-tight text-slate-900 mb-2">Apoiadores do Projeto</h2>
    <p class="text-sm text-slate-600 mb-4">Este projeto é possível graças ao apoio de pessoas e organizações que acreditam na inclusão digital.</p>
    <div class="flex justify-center">
      <%= link_to 'Quero Apoiar', new_supporter_path, class: 'inline-flex items-center rounded-md bg-brand-500 hover:bg-brand-600 text-white px-5 py-2 text-sm font-medium shadow' %>
    </div>
  </div>
  <% if @supporters.any? %>
    <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3 max-w-4xl mx-auto" data-animate-stagger>
      <% @supporters.each do |supporter| %>
        <%= link_to supporter_path(supporter), class: 'block rounded-xl bg-gradient-to-br from-brand-50 to-white border border-brand-200 p-6 shadow-sm hover:shadow-md transition text-center' do %>
          <h3 class="font-bold text-slate-900 text-lg mb-1"><%= supporter.name %></h3>
          <span class="inline-block text-[10px] uppercase tracking-wide font-bold bg-brand-100 text-brand-700 px-3 py-1 rounded mb-2"><%= supporter.support_type %></span>
          <p class="text-xs text-slate-600 italic">Apoiando a inclusão digital</p>
        <% end %>
      <% end %>
    </div>
  <% else %>
    <div class="max-w-md mx-auto rounded-xl bg-slate-50 border border-slate-200 p-8 text-center">
      <p class="text-sm text-slate-600 mb-4">Ainda não temos apoiadores cadastrados.</p>
      <%= link_to 'Seja o primeiro apoiador', new_supporter_path, class: 'inline-flex items-center rounded-md bg-brand-500 hover:bg-brand-600 text-white px-4 py-2 text-sm font-medium shadow' %>
    </div>
  <% end %>
</div>
